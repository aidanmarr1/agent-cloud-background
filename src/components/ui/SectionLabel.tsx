'use client'

export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <h3 className="mb-2.5 text-[10.5px] font-bold uppercase tracking-[0.14em] text-text-tertiary">{children}</h3>
  )
}
